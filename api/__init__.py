"""REST API gateway for SEIM.

This package is a URL aggregator: ``api.urls`` mounts domain viewsets from
``accounts``, ``exchange``, ``documents``, ``notifications``, ``analytics``,
and ``workflows``. It does not define models or viewsets of its own.
"""
