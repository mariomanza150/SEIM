import pytest

from core.models import *


@pytest.mark.django_db
def test_core_model_str():
    # Replace CoreModel with actual model class
    # obj = CoreModel.objects.create(field='value')
    # assert str(obj) == 'value'
    pass  # Placeholder if no model exists
