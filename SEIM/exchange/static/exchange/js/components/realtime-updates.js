/**
 * Real-time Updates for SGII DataTables
 * Provides WebSocket-based real-time data updates and live notifications
 */

class SGIIRealtimeUpdates {
    constructor(tableId, websocketUrl, options = {}) {
        this.table = $(tableId).DataTable();
        this.tableId = tableId;
        this.websocketUrl = websocketUrl;
        this.websocket = null;
        this.reconnectAttempts = 0;
        this.maxReconnectAttempts = 5;
        this.reconnectDelay = 1000; // Start with 1 second
        this.isConnected = false;
        this.heartbeatInterval = null;
        this.updateQueue = [];
        this.processingUpdates = false;
        
        this.options = {
            enableHeartbeat: true,
            heartbeatInterval: 30000, // 30 seconds
            enableQueueing: true,
            batchUpdateDelay: 500, // Batch updates for 500ms
            enableNotifications: true,
            enableVisualFeedback: true,
            ...options
        };
        
        this.eventHandlers = {
            'exchange_updated': this.handleExchangeUpdate.bind(this),
            'bulk_action_progress': this.handleBulkActionProgress.bind(this),
            'new_application': this.handleNewApplication.bind(this),
            'document_uploaded': this.handleDocumentUpload.bind(this),
            'status_changed': this.handleStatusChange.bind(this),
            'assignment_changed': this.handleAssignmentChange.bind(this),
            'notification': this.handleNotification.bind(this)
        };
        
        this.init();
    }
    
    init() {
        this.connectWebSocket();
        this.bindTableEvents();
        this.setupVisibilityHandling();
        this.setupConnectionIndicator();
        
        console.log('SGII Real-time Updates initialized for table:', this.tableId);
    }
    
    connectWebSocket() {
        if (!this.websocketUrl) {
            console.warn('WebSocket URL not provided, real-time updates disabled');
            return;
        }
        
        try {
            // Add authentication token if available
            const token = this.getAuthToken();
            const url = token ? `${this.websocketUrl}?token=${token}` : this.websocketUrl;
            
            this.websocket = new WebSocket(url);
            
            this.websocket.onopen = () => {
                console.log('Real-time updates connected');
                this.isConnected = true;
                this.reconnectAttempts = 0;
                this.reconnectDelay = 1000;
                this.showConnectionStatus('connected');
                this.startHeartbeat();
                
                // Send initial subscription message
                this.subscribe();
            };\n            \n            this.websocket.onmessage = (event) => {\n                try {\n                    const data = JSON.parse(event.data);\n                    this.handleRealtimeMessage(data);\n                } catch (error) {\n                    console.error('Failed to parse WebSocket message:', error);\n                }\n            };\n            \n            this.websocket.onclose = (event) => {\n                console.log('WebSocket connection closed:', event.code, event.reason);\n                this.isConnected = false;\n                this.stopHeartbeat();\n                this.showConnectionStatus('disconnected');\n                this.handleReconnection(event);\n            };\n            \n            this.websocket.onerror = (error) => {\n                console.error('WebSocket error:', error);\n                this.showConnectionStatus('error');\n            };\n            \n        } catch (error) {\n            console.error('WebSocket connection failed:', error);\n            this.showConnectionStatus('error');\n            this.handleReconnection();\n        }\n    }\n    \n    handleRealtimeMessage(data) {\n        if (data.type === 'heartbeat') {\n            this.handleHeartbeat(data);\n            return;\n        }\n        \n        // Queue updates if enabled\n        if (this.options.enableQueueing) {\n            this.updateQueue.push(data);\n            this.processUpdateQueue();\n        } else {\n            this.processUpdate(data);\n        }\n    }\n    \n    processUpdateQueue() {\n        if (this.processingUpdates) {\n            return;\n        }\n        \n        this.processingUpdates = true;\n        \n        setTimeout(() => {\n            const updates = [...this.updateQueue];\n            this.updateQueue = [];\n            \n            // Group updates by type and ID for efficient processing\n            const groupedUpdates = this.groupUpdates(updates);\n            \n            // Process grouped updates\n            Object.values(groupedUpdates).forEach(update => {\n                this.processUpdate(update);\n            });\n            \n            this.processingUpdates = false;\n        }, this.options.batchUpdateDelay);\n    }\n    \n    groupUpdates(updates) {\n        const grouped = {};\n        \n        updates.forEach(update => {\n            const key = `${update.type}_${update.id || 'general'}`;\n            grouped[key] = update; // Latest update wins\n        });\n        \n        return grouped;\n    }\n    \n    processUpdate(data) {\n        const handler = this.eventHandlers[data.type];\n        if (handler) {\n            handler(data);\n        } else {\n            console.warn('No handler for update type:', data.type);\n        }\n        \n        // Show visual feedback\n        if (this.options.enableVisualFeedback) {\n            this.showUpdateFeedback(data.type);\n        }\n    }\n    \n    handleExchangeUpdate(data) {\n        const exchangeId = data.exchange_id;\n        const changes = data.changes;\n        \n        // Find and update the specific table row\n        const row = this.findTableRow(exchangeId);\n        if (row) {\n            this.updateTableRow(row, changes);\n            this.highlightRow(row, 'updated');\n        }\n        \n        // Show notification if enabled\n        if (this.options.enableNotifications) {\n            this.showNotification(\n                `Exchange #${exchangeId} updated`,\n                `Status: ${changes.status}`,\n                'info'\n            );\n        }\n    }\n    \n    handleBulkActionProgress(data) {\n        // Update bulk action progress if modal is open\n        if ($('#bulkProgressModal').hasClass('show')) {\n            const progress = data.progress;\n            this.updateBulkActionProgress(progress);\n        }\n    }\n    \n    handleNewApplication(data) {\n        const exchange = data.exchange;\n        \n        // Add new row to table if it matches current filters\n        if (this.shouldShowExchange(exchange)) {\n            this.addNewTableRow(exchange);\n            this.showNotification(\n                'New Application',\n                `${exchange.student_name} submitted a new application`,\n                'success'\n            );\n        }\n        \n        // Update statistics\n        this.updateTableStats();\n    }\n    \n    handleDocumentUpload(data) {\n        const documentData = data.document;\n        const exchangeId = data.exchange_id;\n        \n        // Update document count for the exchange row\n        const row = this.findTableRow(exchangeId);\n        if (row) {\n            this.updateDocumentCount(row, documentData);\n        }\n        \n        if (this.options.enableNotifications) {\n            this.showNotification(\n                'Document Uploaded',\n                `New document uploaded for Exchange #${exchangeId}`,\n                'info'\n            );\n        }\n    }\n    \n    handleStatusChange(data) {\n        const exchangeId = data.exchange_id;\n        const newStatus = data.new_status;\n        const oldStatus = data.old_status;\n        \n        const row = this.findTableRow(exchangeId);\n        if (row) {\n            this.updateRowStatus(row, newStatus);\n            this.highlightRow(row, 'status-changed');\n        }\n        \n        if (this.options.enableNotifications) {\n            this.showNotification(\n                'Status Changed',\n                `Exchange #${exchangeId}: ${oldStatus} → ${newStatus}`,\n                this.getStatusNotificationType(newStatus)\n            );\n        }\n    }\n    \n    handleAssignmentChange(data) {\n        const exchangeId = data.exchange_id;\n        const assignedTo = data.assigned_to;\n        \n        const row = this.findTableRow(exchangeId);\n        if (row) {\n            this.updateRowAssignment(row, assignedTo);\n            this.highlightRow(row, 'assigned');\n        }\n        \n        if (this.options.enableNotifications) {\n            this.showNotification(\n                'Assignment Changed',\n                `Exchange #${exchangeId} assigned to ${assignedTo}`,\n                'info'\n            );\n        }\n    }\n    \n    handleNotification(data) {\n        this.showNotification(\n            data.title || 'Notification',\n            data.message,\n            data.type || 'info'\n        );\n    }\n    \n    findTableRow(exchangeId) {\n        let targetRow = null;\n        \n        this.table.rows().every(function() {\n            const data = this.data();\n            if (data && data[0] == exchangeId) { // First column should be ID\n                targetRow = this;\n                return false; // Break the loop\n            }\n            return true;\n        });\n        \n        return targetRow;\n    }\n    \n    updateTableRow(row, changes) {\n        const data = row.data();\n        \n        // Update data based on changes\n        Object.keys(changes).forEach(key => {\n            switch(key) {\n                case 'status':\n                    // Update status column (assuming column 6 is status)\n                    data[6] = this.generateStatusBadge(changes[key]);\n                    break;\n                case 'destination_university':\n                    data[2] = changes[key];\n                    break;\n                // Add more cases as needed\n            }\n        });\n        \n        // Redraw the row\n        row.data(data).draw(false);\n    }\n    \n    addNewTableRow(exchange) {\n        // Format exchange data for table row\n        const rowData = this.formatExchangeForTable(exchange);\n        \n        // Add to table\n        const newRow = this.table.row.add(rowData).draw(false);\n        \n        // Highlight the new row\n        setTimeout(() => {\n            $(newRow.node()).addClass('row-inserted');\n        }, 100);\n    }\n    \n    highlightRow(row, type = 'updated') {\n        const $row = $(row.node());\n        \n        // Remove existing highlight classes\n        $row.removeClass('row-updated row-inserted row-status-changed row-assigned');\n        \n        // Add new highlight class\n        setTimeout(() => {\n            $row.addClass(`row-${type}`);\n        }, 50);\n        \n        // Remove highlight after animation\n        setTimeout(() => {\n            $row.removeClass(`row-${type}`);\n        }, 2000);\n    }\n    \n    updateBulkActionProgress(progress) {\n        $('#progressCurrent').text(progress.current);\n        $('#progressTotal').text(progress.total);\n        \n        const percentage = Math.round((progress.current / progress.total) * 100);\n        $('#bulkProgressBar').css('width', `${percentage}%`).attr('aria-valuenow', percentage);\n        \n        if (progress.message) {\n            $('#progressAction').text(progress.message);\n        }\n    }\n    \n    showNotification(title, message, type = 'info') {\n        // Use existing notification system or create toast\n        if (window.showNotification) {\n            window.showNotification(title, message, type);\n        } else {\n            this.createToast(title, message, type);\n        }\n    }\n    \n    createToast(title, message, type) {\n        const toastHtml = `\n            <div class=\"toast align-items-center text-white bg-${type} border-0\" role=\"alert\">\n                <div class=\"d-flex\">\n                    <div class=\"toast-body\">\n                        <strong>${title}</strong><br>\n                        ${message}\n                    </div>\n                    <button type=\"button\" class=\"btn-close btn-close-white me-2 m-auto\" \n                            data-bs-dismiss=\"toast\"></button>\n                </div>\n            </div>\n        `;\n        \n        // Create toast container if it doesn't exist\n        let toastContainer = document.getElementById('toastContainer');\n        if (!toastContainer) {\n            toastContainer = document.createElement('div');\n            toastContainer.id = 'toastContainer';\n            toastContainer.className = 'toast-container position-fixed top-0 end-0 p-3';\n            toastContainer.style.zIndex = '9999';\n            document.body.appendChild(toastContainer);\n        }\n        \n        // Add toast\n        toastContainer.insertAdjacentHTML('beforeend', toastHtml);\n        \n        // Show toast\n        const toastElement = toastContainer.lastElementChild;\n        const toast = new bootstrap.Toast(toastElement, { delay: 5000 });\n        toast.show();\n        \n        // Remove toast element after it's hidden\n        toastElement.addEventListener('hidden.bs.toast', () => {\n            toastElement.remove();\n        });\n    }\n    \n    showUpdateFeedback(updateType) {\n        // Show subtle visual feedback for updates\n        const indicator = document.getElementById('realtimeIndicator');\n        if (indicator) {\n            indicator.classList.add('active');\n            setTimeout(() => {\n                indicator.classList.remove('active');\n            }, 1000);\n        }\n    }\n    \n    subscribe() {\n        if (this.isConnected) {\n            const subscription = {\n                type: 'subscribe',\n                channels: ['exchanges', 'documents', 'notifications'],\n                user_id: this.getCurrentUserId(),\n                table_id: this.tableId\n            };\n            \n            this.websocket.send(JSON.stringify(subscription));\n        }\n    }\n    \n    startHeartbeat() {\n        if (!this.options.enableHeartbeat) return;\n        \n        this.heartbeatInterval = setInterval(() => {\n            if (this.isConnected) {\n                this.websocket.send(JSON.stringify({ type: 'heartbeat' }));\n            }\n        }, this.options.heartbeatInterval);\n    }\n    \n    stopHeartbeat() {\n        if (this.heartbeatInterval) {\n            clearInterval(this.heartbeatInterval);\n            this.heartbeatInterval = null;\n        }\n    }\n    \n    handleHeartbeat(data) {\n        // Server responds to heartbeat, connection is alive\n        console.debug('Heartbeat received from server');\n    }\n    \n    handleReconnection(event) {\n        if (this.reconnectAttempts >= this.maxReconnectAttempts) {\n            console.error('Max reconnection attempts reached');\n            this.showConnectionStatus('failed');\n            return;\n        }\n        \n        this.reconnectAttempts++;\n        this.showConnectionStatus('reconnecting');\n        \n        setTimeout(() => {\n            console.log(`Attempting to reconnect (${this.reconnectAttempts}/${this.maxReconnectAttempts})...`);\n            this.connectWebSocket();\n        }, this.reconnectDelay);\n        \n        // Exponential backoff\n        this.reconnectDelay = Math.min(this.reconnectDelay * 2, 30000);\n    }\n    \n    setupVisibilityHandling() {\n        // Pause/resume real-time updates based on page visibility\n        document.addEventListener('visibilitychange', () => {\n            if (document.hidden) {\n                this.pauseUpdates();\n            } else {\n                this.resumeUpdates();\n            }\n        });\n    }\n    \n    pauseUpdates() {\n        this.stopHeartbeat();\n        // Don't close WebSocket, just pause heartbeat\n        console.log('Real-time updates paused (page hidden)');\n    }\n    \n    resumeUpdates() {\n        if (this.isConnected) {\n            this.startHeartbeat();\n            // Refresh table data in case we missed updates\n            this.table.ajax.reload(null, false);\n        } else {\n            this.connectWebSocket();\n        }\n        console.log('Real-time updates resumed');\n    }\n    \n    showConnectionStatus(status) {\n        const statusMap = {\n            connected: { text: 'Connected', class: 'text-success', icon: 'bi-wifi' },\n            disconnected: { text: 'Disconnected', class: 'text-warning', icon: 'bi-wifi-off' },\n            reconnecting: { text: 'Reconnecting...', class: 'text-info', icon: 'bi-arrow-repeat' },\n            error: { text: 'Connection Error', class: 'text-danger', icon: 'bi-exclamation-triangle' },\n            failed: { text: 'Connection Failed', class: 'text-danger', icon: 'bi-x-circle' }\n        };\n        \n        const config = statusMap[status] || statusMap.disconnected;\n        \n        // Update connection indicator if it exists\n        const indicator = document.getElementById('connectionStatus');\n        if (indicator) {\n            indicator.innerHTML = `\n                <i class=\"${config.icon} me-1\"></i>\n                <span class=\"${config.class}\">${config.text}</span>\n            `;\n        }\n    }\n    \n    setupConnectionIndicator() {\n        // Create connection status indicator if it doesn't exist\n        if (!document.getElementById('connectionStatus')) {\n            const indicator = document.createElement('div');\n            indicator.id = 'connectionStatus';\n            indicator.className = 'position-fixed bottom-0 end-0 p-2 small';\n            indicator.style.zIndex = '1000';\n            document.body.appendChild(indicator);\n        }\n        \n        // Create realtime update indicator\n        if (!document.getElementById('realtimeIndicator')) {\n            const indicator = document.createElement('div');\n            indicator.id = 'realtimeIndicator';\n            indicator.className = 'realtime-indicator';\n            indicator.innerHTML = '<i class=\"bi bi-broadcast\"></i>';\n            document.body.appendChild(indicator);\n        }\n    }\n    \n    bindTableEvents() {\n        // Listen for table events that might need real-time updates\n        this.table.on('draw.dt', () => {\n            // Re-subscribe when table is redrawn\n            if (this.isConnected) {\n                this.subscribe();\n            }\n        });\n    }\n    \n    // Utility methods\n    getAuthToken() {\n        return document.querySelector('meta[name=\"auth-token\"]')?.getAttribute('content') ||\n               localStorage.getItem('auth_token');\n    }\n    \n    getCurrentUserId() {\n        return document.querySelector('meta[name=\"user-id\"]')?.getAttribute('content') ||\n               window.currentUserId;\n    }\n    \n    generateStatusBadge(status) {\n        const statusColors = {\n            'DRAFT': 'bg-light text-muted border',\n            'SUBMITTED': 'bg-warning-subtle text-warning border border-warning-subtle',\n            'UNDER_REVIEW': 'bg-info-subtle text-info border border-info-subtle',\n            'APPROVED': 'bg-success-subtle text-success border border-success-subtle',\n            'REJECTED': 'bg-danger-subtle text-danger border border-danger-subtle',\n            'COMPLETED': 'bg-secondary-subtle text-secondary border border-secondary-subtle'\n        };\n        \n        const colorClass = statusColors[status] || statusColors['DRAFT'];\n        return `<span class=\"badge ${colorClass}\">${status.replace('_', ' ')}</span>`;\n    }\n    \n    getStatusNotificationType(status) {\n        const typeMap = {\n            'APPROVED': 'success',\n            'REJECTED': 'danger',\n            'SUBMITTED': 'info',\n            'UNDER_REVIEW': 'info',\n            'COMPLETED': 'success'\n        };\n        return typeMap[status] || 'info';\n    }\n    \n    shouldShowExchange(exchange) {\n        // Check if the exchange matches current table filters\n        // This is a simplified implementation\n        return true;\n    }\n    \n    formatExchangeForTable(exchange) {\n        // Format exchange object for DataTable row\n        return [\n            exchange.id,\n            exchange.student_name,\n            exchange.destination_university || '-',\n            exchange.destination_country || '-',\n            exchange.exchange_program || '-',\n            this.formatDuration(exchange.start_date, exchange.end_date),\n            this.generateStatusBadge(exchange.status),\n            new Date(exchange.created_at).toLocaleDateString(),\n            this.generateActionButtons(exchange)\n        ];\n    }\n    \n    formatDuration(startDate, endDate) {\n        if (!startDate || !endDate) {\n            return '<span class=\"text-muted\">Not specified</span>';\n        }\n        const start = new Date(startDate);\n        const end = new Date(endDate);\n        return `${start.toLocaleDateString('en-US', { month: 'short', year: 'numeric' })} - ${end.toLocaleDateString('en-US', { month: 'short', year: 'numeric' })}`;\n    }\n    \n    generateActionButtons(exchange) {\n        // Generate action buttons HTML - simplified\n        return `\n            <div class=\"btn-group btn-group-sm\">\n                <a href=\"/exchange/${exchange.id}/\" class=\"btn btn-outline-primary btn-sm\">\n                    <i class=\"bi bi-eye\"></i>\n                </a>\n            </div>\n        `;\n    }\n    \n    updateTableStats() {\n        // Update table statistics in real-time\n        // This could update counts, charts, etc.\n    }\n    \n    disconnect() {\n        if (this.websocket) {\n            this.websocket.close();\n        }\n        this.stopHeartbeat();\n        this.isConnected = false;\n    }\n    \n    destroy() {\n        this.disconnect();\n        // Clean up event listeners, etc.\n        document.removeEventListener('visibilitychange', this.handleVisibilityChange);\n    }\n}\n\n// Global instance variable\nlet sgiiRealtimeUpdates = null;\n\n// Auto-initialization function\nfunction initializeSGIIRealtimeUpdates(tableId, websocketUrl, options = {}) {\n    if (sgiiRealtimeUpdates) {\n        console.warn('SGII Real-time Updates already initialized');\n        return sgiiRealtimeUpdates;\n    }\n    \n    sgiiRealtimeUpdates = new SGIIRealtimeUpdates(tableId, websocketUrl, options);\n    return sgiiRealtimeUpdates;\n}\n\n// Export for module usage\nif (typeof module !== 'undefined' && module.exports) {\n    module.exports = SGIIRealtimeUpdates;\n}