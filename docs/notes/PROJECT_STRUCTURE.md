# SEIM Project Structure Guide

**Last Updated:** 2025-12-05  
**Purpose:** Comprehensive guide to project directory structure and organization

---

## Overview

SEIM follows a clear organizational structure to separate concerns and make the codebase maintainable:

- **Application Code** - Django apps and core functionality
- **Documentation** - Single tree in `docs/` (manuals + Sphinx + `notes/`). `documents/` is a Django app, not docs.
- **Configuration** - Environment, Docker, and tool configurations
- **Tests** - Comprehensive test suite
- **Tools** - AI agent configurations and IDE settings

---

## Root Directory Structure

```
SEIM/
├── accounts/              # User management and authentication
├── analytics/             # Analytics and reporting
├── api/                   # API routing and organization
├── application_forms/     # Dynamic form builder
├── cms/                   # Wagtail CMS integration
├── core/                  # Shared utilities and base classes
├── documents/             # Document management
├── exchange/              # Exchange programs and applications
├── frontend-vue/          # Vue 3 SPA (served at /seim/)
├── grades/                # Grade translation system
├── notifications/         # Notification system
├── seim/                  # Django project settings
├── static/                # Static files (CSS, JS, images)
├── templates/             # Django templates
├── tests/                 # Test suite
├── scripts/               # Utility scripts
├── nginx/                 # Nginx configuration
├── locale/                # Translation files
├── docs/                  # Deprecated pointer → docs/ (see docs/README.md)
├── docs/         # Canonical manuals + Sphinx + notes/
├── documents/             # Django document-upload app (not docs)
├── LICENSE                # MIT
├── .tools/                # AI agent configs and IDE settings
├── docker-compose.yml     # Main Docker Compose configuration
├── Dockerfile             # Main Dockerfile
├── Makefile               # Development commands
├── README.md              # Project overview and quick start
├── pyproject.toml         # Tooling + Python dependency pins (SSOT)
└── branding/              # Institution packs (UAdeC default)
```

---

## Documentation Structure

### `docs/notes/` - Generated notes and working papers

**Purpose:** Generated analysis, status reports, and historical Vue/CMS notes. Not the contributor manual. See [notes/README.md](README.md).

**Canonical manuals:** [`docs/`](../README.md) (includes Sphinx).

**Contents:**
- `architecture.md` - Auto-generated system architecture
- `api-contracts.md` - Auto-generated API documentation
- `data-models.md` - Auto-generated database schema
- `component-inventory.md` - Auto-generated component catalog
- `source-tree-analysis.md` - Auto-generated directory analysis
- `project-overview.md` - Auto-generated project summary
- `development-guide.md` - Auto-generated development guide
- `index.md` - Index of generated documentation

**Subdirectories:**
- `docs/notes/status/` - Status reports and progress tracking
- `docs/notes/guides/` - Quick reference guides
- `docs/notes/analysis/` - Analysis documents

**When to Use:**
- Reference for understanding current system state
- Quick lookup for API contracts and data models
- Understanding component structure

**Maintenance:**
- Regenerated by tools (BMAD document-project workflow, etc.)
- Do not manually edit (changes will be overwritten)
- If updates needed, modify source code and regenerate

### `docs/` - Manual/Maintained Documentation

**Purpose:** Documentation that is manually written and maintained by the team.

**Contents:**
- `README.md` - Documentation index
- `architecture.md` - Manual architecture documentation
- `developer_guide.md` - Developer guide
- `api_documentation.md` - Manual API documentation
- `installation.md` - Installation instructions
- `deployment.md` - Deployment guide
- `testing.md` - Testing guide
- `business_rules.md` - Business logic documentation
- `roadmap.md` - Development roadmap
- `backlog.md` - Current tasks
- `user_stories.md` - User requirements

**Subdirectories:**
- `docs/notes/` - Working notes formerly in `docs/`
- `docs/archive/` - Historical documentation
- `docs/audit_reports/` - Audit reports
- `docs/implementation_plans/` - Implementation plans
- `docs/wireframes/` - UI/UX wireframes
- `docs/sphinx/` - Sphinx documentation source

**When to Use:**
- Authoritative source for how to use the system
- Development guidelines and best practices
- Planning and roadmap information
- User guides and tutorials

**Maintenance:**
- Manually updated by developers
- Should be kept up-to-date with code changes
- Reviewed during code reviews

---

## Tool Configuration Structure

### `.tools/` - AI Agent and IDE Configurations

**Purpose:** Consolidates all tool-specific configuration directories.

**Structure:**
```
.tools/
├── agents/              # AI agent configurations
│   ├── .agent/
│   ├── .agentvibes/
│   ├── .augment/
│   ├── .bmad/          # BMAD Method workflows and agents
│   ├── .claude/        # Claude AI configurations
│   ├── .clinerules/
│   ├── .codex/
│   ├── .crush/
│   ├── .gemini/
│   ├── .iflow/
│   ├── .opencode/
│   ├── .qwen/
│   ├── .rovodev/
│   └── .trae/
└── ide-config/         # IDE-specific configurations
    ├── .cursor/        # Cursor IDE settings
    └── .windsurf/      # Windsurf IDE settings
```

**Purpose:**
- Keeps root directory clean
- Makes it clear these are tool configurations
- Easier to manage and document
- Can be excluded from certain operations

**Note:** These directories are typically hidden (start with `.`) and contain tool-specific settings. They don't affect application functionality.

---

## Application Code Structure

### Django Apps

Each Django app follows a standard structure:

```
app_name/
├── __init__.py
├── admin.py           # Django admin configuration
├── apps.py            # App configuration
├── models.py          # Database models
├── views.py           # View functions/classes
├── serializers.py     # DRF serializers
├── services.py        # Business logic (service layer)
├── urls.py            # URL routing
├── filters.py         # Query filters (if applicable)
├── tasks.py           # Celery tasks (if applicable)
├── migrations/        # Database migrations
├── tests/             # App-specific tests
└── management/        # Management commands (if applicable)
    └── commands/
```

### Key Apps

- **`accounts/`** - User management, authentication, profiles
- **`exchange/`** - Exchange programs and applications
- **`documents/`** - Document upload and validation
- **`notifications/`** - Email and in-app notifications
- **`analytics/`** - Reporting and metrics
- **`grades/`** - Grade translation system
- **`cms/`** - Wagtail CMS integration
- **`core/`** - Shared utilities, permissions, caching

---

## Configuration Files

### Root-Level Configuration

- **`docker-compose.yml`** - Main Docker Compose configuration
- **`docker-compose.e2e.yml`** - E2E testing configuration
- **`docker-compose.local-prod.yml`** - Local production setup
- **`docker-compose.prod.yml`** - Production configuration
- **`Dockerfile`** - Main application Dockerfile
- **`Dockerfile.e2e`** - E2E testing Dockerfile
- **`Makefile`** - Development commands
- **`pyproject.toml`** - Python dependency pins (source of truth; extras: dev, test, docs)
- **`branding/`** - Institution packs (UAdeC default example)
- **`pytest.ini`** - Pytest configuration
- **`frontend-vue/`** - Vue 3 SPA (Vite + Vitest), served at `/seim/`
- **`.gitignore`** - Git ignore patterns
- **`.env.example`** - Environment variable template

### Settings Structure

```
seim/
├── settings/
│   ├── base.py         # Common settings
│   ├── development.py  # Development settings
│   ├── production.py   # Production settings
│   └── test.py         # Test settings
├── urls.py             # Main URL configuration
├── wsgi.py             # WSGI configuration
└── asgi.py             # ASGI configuration
```

---

## Test Structure

```
tests/
├── conftest.py                    # Pytest configuration and fixtures
├── unit/                          # Unit tests
│   ├── accounts/
│   ├── analytics/
│   ├── documents/
│   ├── exchange/
│   ├── grades/
│   ├── notifications/
│   └── ...
├── integration/                   # Integration tests
│   ├── api/
│   └── test_complete_workflows.py
├── e2e_playwright/                # Playwright E2E tests
│   ├── conftest.py
│   ├── test_video_demos.py
│   └── videos/                    # Generated video demos
└── selenium/                      # Selenium E2E tests
    └── test_core_functionality.py
```

---

## Static Files and Templates

### Static Files

```
static/
├── css/                  # CMS / shared styles
│   ├── main.css
│   └── utilities/seim-shared-tokens.css
└── js/                   # CMS-only scripts (SPA JS is in frontend-vue/)
```

### Templates

```
templates/
├── base.html              # Base template
├── components/            # Reusable components
│   ├── navigation/
│   └── ...
├── vue_spa_missing_dist/  # Dev placeholder when Vue dist is missing
└── admin/                 # Admin templates
```

App UI pages live in `frontend-vue/`, not `templates/frontend/`.

---

## Scripts and Utilities

```
scripts/
├── download_institution_assets.py
├── download_uadec_assets.py          # compatibility wrapper
├── seed_e2e_test_data.py
├── run_video_demos.sh
├── run_video_demos.ps1
└── ...
```

---

## Best Practices

### Adding New Files

1. **Documentation:**
   - Generated docs → `docs/notes/`
   - Manual docs → `docs/`
   - Status/Progress → `docs/notes/status/`
   - Guides → `docs/` or `docs/notes/guides/`

2. **Configuration:**
   - Tool configs → `.tools/agents/` or `.tools/ide-config/`
   - Environment → `.env` (not committed)
   - Docker → Root level with descriptive names

3. **Code:**
   - Follow Django app structure
   - Place in appropriate app directory
   - Add tests in `tests/` directory

### Finding Documentation

- **Need authoritative info?** → Check `docs/`
- **Need current system state?** → Check `docs/notes/`
- **Need quick reference?** → Check `docs/` or `docs/notes/guides/`
- **Need status/progress?** → Check `docs/notes/status/`

### Maintenance

- Keep root directory clean (< 10 visible files)
- Consolidate tool configurations
- Separate generated vs manual documentation
- Update this guide when structure changes

---

## Quick Reference

| Need | Location |
|------|----------|
| API documentation | `docs/notes/api-contracts.md` (generated) or `docs/api_documentation.md` (manual) |
| Architecture | `docs/architecture.md` |
| Developer guide | `docs/developer_guide.md` |
| Installation | `docs/installation.md` |
| Deployment | `docs/deployment.md` |
| Testing | `docs/testing.md` |
| Status reports | `docs/notes/status/` |
| Quick guides | `docs/` or `docs/notes/guides/` |
| Project overview | `README.md` |
| Tool configs | `.tools/agents/` or `.tools/ide-config/` |

---

**Maintained by:** Development Team  
**Last Reviewed:** 2025-12-05
