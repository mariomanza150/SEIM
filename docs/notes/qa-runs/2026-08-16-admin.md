# Manual QA — Admin (Sections 6, 7, 4.8)

**Date:** 2026-08-16  
**Stack:** Compose project `seim-localprod` (`docker-compose.local-prod.yml`)  
**App:** `http://localhost:8020`  
**Role:** Admin `admin@test.com` / `admin123` (Django admin username is `admin`)  
**Method:** HTTP (`curl.exe` + Django session) for `/seim/django-admin/`, `/cms/`, `/data-management/`, `/dashboard/analytics/`, `/api/docs/`, `/grades/api/scales/`. Puppeteer (system Chrome) for SPA after coordinator’s default Puppeteer Chrome cache was missing. Stopped Puppeteer once the session landed on staff exchange-agreements (coordinator surface).  
**Containers:** `seim-web-local-prod` healthy, `0.0.0.0:8020->8000`.

**Env (from Public QA; confirmed live):** container `/app/exchange/urls.py` is a stub (`urlpatterns = []`). Host source has the real router. `GET /api/programs/`, `/api/applications/`, `/api/partner/agreements/` return Wagtail HTML 404. SPA pages that depend on those APIs are **Blocked (env)**, not Fail. Did not rebuild/fix the image.

## Counts

| Result | Count |
|--------|------:|
| Pass | 9 |
| Fail | 1 |
| Blocked | 2 |
| Skipped | 1 (`6.7` — student/coordinator; other agents) |

**Pass:** 6.2, 6.3, 6.4, 6.5, 4.8, 7.2, 7.3, 7.4, 7.5  
**Fail:** 7.1  
**Blocked:** 6.1, 6.6

---

## Section 6 — Admin SPA console

Navbar Admin console present after JWT login: Programs, Forms, Form builders, Data management, Workflows. User “Admin User”. SPA is real (`SEIM · 2.0.0-vue`), not “Vue build required”.

### 6.1 Admin programs (`admin-console`) — **Blocked** (env — stub `exchange/urls.py`)

- **URLs:** `/seim/admin` → `/seim/admin/programs`
- **Saw:** Chrome **Pass** (`Admin — Programs`, Program management, Search/Active/Sort, New program). List **Blocked:** alert `Failed to load programs.`
- **API:** `GET /api/programs/` → `404` Wagtail HTML (179 bytes). Same root cause as Public **DEF-2026-08-16-01**.
- **Repro:**
  1. Login `admin@test.com` / `admin123` at `http://localhost:8020/seim/login`.
  2. Open `http://localhost:8020/seim/admin/programs` (or `/seim/admin`).
  3. Page chrome renders; body shows **Failed to load programs.**
  4. `curl.exe http://localhost:8020/api/programs/` → HTML 404, not DRF JSON.

### 6.2 Admin forms (`dynamic-forms`) — **Pass**

- **URL:** `/seim/admin/forms`
- **Saw:** Form management table. Row **Manual QA Application Form** (`application`, Active Yes, 0 fields). Refresh / New form / type filters present. No load error.

### 6.3 Dynforms list + editor (`dynamic-forms`) — **Pass**

- **URLs:** `/seim/admin/dynforms` → `/seim/admin/dynforms/1`
- **Saw:** List title **Admin — Form builders**; same form; **Open builder**. Editor title **Admin — Visual form builder**; field palette (Text, Textarea, Email, Number, Date, Select, Checkbox, Radio, File); empty canvas “No fields yet”; Preview; Save. Did not publish.

### 6.4 Data management SPA (`data-management`) — **Pass**

- **URL:** `/seim/admin/data-management` (lands `?section=bulk_operation`)
- **Saw:** Sections: Bulk operations, Data export, Data import, Demo data setup, Database reset (“requires confirmation”), Data cleanup. “No configured items” / “No operations logged yet.” Did **not** run reset or cleanup.
- **API:** `GET /api/data-management/catalog/` `200`; `GET /api/data-management/resources/?section=data_export` `200`; logs empty `200`.

### 6.5 Workflows + editor (`admin-console`) — **Pass**

- **URLs:** `/seim/admin/workflows` → `/seim/admin/workflows/019c74e4-5434-4e20-84b7-887db0cf6ebb`
- **Saw:** **Manual QA Review Workflow** (active, latest published —). Editor: Start / Process_1, Validate / Save / Publish, `v1 — draft`. Read-only; did not publish.

### 6.6 Admin application edit (`admin-console`) — **Blocked** (env — stub `exchange/urls.py`)

- **URL:** `/seim/admin/applications/e878ed55-6c94-491a-af22-d40843e311cb`  
  UUID from Django admin Applications changelist (valid id, 1 row).
- **Saw:** Chrome **Admin — Application**; alert `Failed to load application.` Not a Vue 404.
- **API:** `GET /api/applications/` → Wagtail HTML 404.
- **Repro:**
  1. Login as admin.
  2. Copy an application UUID from `/seim/django-admin/exchange/application/` (example `e878ed55-6c94-491a-af22-d40843e311cb`).
  3. Open `/seim/admin/applications/<id>`.
  4. **Failed to load application.**

### 6.7 Non-admin redirected — **Skipped**

Requires student (and coordinator) login. Student agent covers student; not executed here.

---

## Section 4.8 — Workload admin (`coord-review`) — **Pass**

- **URL:** `/seim/coordinator-workload` as admin
- **Saw:** **Your workload** (all 0 / —) **and** **Institution overview (admin):** Total pending review 0, Unassigned 0, Stale under review (>14 days) 0, table **Pending by assigned coordinator** with **Coordinator User / 0**.
- **API:** `GET /api/accounts/dashboard/coordinator-workload/` `200` with `you`, `global` (incl. `stale_under_review_14d`), `distribution`.

---

## Section 7 — Django / Wagtail / SSR / grades

### 7.1 Django admin (`url-routing`) — **Fail** (agreements changelist 500)

- **Paths:** `/admin/` and `/django/admin/` → `302` `/seim/django-admin/`. Unauthenticated `/seim/django-admin/` → `/seim/django-admin/login/?next=…`.
- **Login:** `username=admin` / `admin123` (email `admin@test.com` did not authenticate on the Django form). Session established.
- **Users:** `/seim/django-admin/accounts/user/` `200`. Rows include `admin@test.com`, `coordinator@test.com`, `student@test.com`, `mmanzano@uadec.edu.mx`, `test@example.com`. **No** `partner@test.com`.
- **Programs:** `/seim/django-admin/exchange/program/` `200`. Rows: **Manual QA Mobility 2026**, **UAdeC Law mobility (Mario-eligible)** (not Erasmus/DAAD/Fulbright seed names).
- **Agreements:** `/seim/django-admin/exchange/exchangeagreement/` → **HTTP 500**.
- **Repro:**
  1. Sign in at `http://localhost:8020/seim/django-admin/login/` as `admin` / `admin123`.
  2. Open Exchange → **Exchange agreements** (`/seim/django-admin/exchange/exchangeagreement/`).
  3. Browser/curl: `500` HTML “Server Error (500)”.
  4. `docker logs seim-web-local-prod`: `django.db.utils.ProgrammingError: column exchange_exchangeagreement.required_gpa does not exist`.
- Distinct from the `exchange/urls.py` stub (this is Django admin ORM vs DB). Users/programs changelists are fine.

### 7.2 Wagtail (`cms-public`) — **Pass** (explorer; marketing pages unrestored)

- **URL:** `/cms/` with the same Django session → Wagtail dashboard (`Dashboard - Wagtail`), not Vue.
- **Page tree:** `/cms/pages/` `200`. Live page **Welcome to your new Wagtail site!** (`/cms/pages/2/`). API `GET /cms/api/main/pages/` `total_count: 1`, children 0.
- Navbar link **Wagtail CMS** on Django admin. No `/programas/` pages (same **restore_cms** gap as Public 0.4–0.6). Explorer itself is Wagtail, not a 404.

### 7.3 Data-management hub redirect (`data-management`) — **Pass**

- Anonymous `GET /data-management/` → `302` `/seim/login/?next=/data-management/`.
- Authenticated Django admin session: `200` hub (Bulk operations, Data export/import, Demo data setup, Database reset with confirmation copy, Data cleanup). Not a 404.
- SPA equivalent `/seim/admin/data-management` also loads (6.4). Checklist allows either.

### 7.4 SSR analytics exports (`analytics`) — **Pass**

- Anonymous `/dashboard/analytics/` → `302` login. Admin session: `200` HTML `<title>Analytics - SEIM</title>`, Export dropdown: Download CSV / Excel (.xlsx) / PDF.
- JWT `GET /api/analytics/export/` → `200` `text/csv` (`analytics-report.csv`, 287 bytes). `?export_format=xlsx` → `200` xlsx 6092 bytes. `?export_format=pdf` → `200` PDF 2391 bytes.
- `/admin-dashboard/` → `302` `/seim/dashboard/` (not 404).

### 7.5 Grades API only (`grades`) — **Pass**

- OpenAPI `/api/schema/` has tag `grades` and paths `/api/grades/scales/`, `/values/`, `/translations/` (plus translate / convert_gpa / check_eligibility).
- Authenticated `GET /grades/api/scales/` `200`, 6 scales (ECTS, US GPA, German, French, UK, Canadian). Canonical `GET /api/grades/` lists scales/values/translations URLs.
- `/seim/grades` is SPA shell `200` (Vue history). Router `frontend-vue/src/router/index.js`: `{ path: 'grades', redirect: { name: 'Profile' } }`. No dedicated grades Vue console. Live client redirect not re-snapshotted after Puppeteer left admin SPA.

---

## Defects

### DEF-2026-08-16-01 (env) — Exchange API missing in `seim-localprod` image

Same as Public QA. **Blocks 6.1 and 6.6** (and partner agreements API). Container `/app/exchange/urls.py` is `urlpatterns = []`.

### DEF-2026-08-16-03 — Django admin Exchange agreements changelist 500 (High)

- **Cluster:** `admin-console` / Django admin
- **Symptom:** `/seim/django-admin/exchange/exchangeagreement/` returns 500.
- **Repro:** see 7.1.
- **Cause (logs):** `column exchange_exchangeagreement.required_gpa does not exist`.
- Not the urls stub; DB schema vs admin model on this stack.

---

## Notes

- Seed on this DB is a thin “Manual QA …” set, not full `seed_demo_readiness` (no partner user, no DEMO-SEED-AGR-*, no Erasmus/Fulbright program names).
- Did not run Section 8. Did not edit `manual-qa-full-checklist.md`, `feature-tracking.md`, or `feature-test-tracking.md`.
- Destructive data-management actions not executed.
- Django admin login field is **username** `admin`, not the email used on SPA JWT login.
