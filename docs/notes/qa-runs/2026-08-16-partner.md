# Manual QA — Partner (Section 5 + staff-URL denial)

**Date:** 2026-08-16  
**Stack:** Compose project `seim-localprod` (`docker-compose.local-prod.yml`)  
**App:** `http://localhost:8020`  
**Role:** Partner `partner@test.com` / `partner123` (attempted)  
**Method:** HTTP JWT `POST /api/login/` and API probes. SPA login as partner not reached (credentials rejected). Did not use student IDE browser. Stopped Puppeteer after admin pass so coordinator could keep `/seim/exchange-agreements`.  
**Env (from Public QA; confirmed live):** container `/app/exchange/urls.py` is `urlpatterns = []`. `GET /api/partner/agreements/` and `/api/exchange-agreements/` are Wagtail HTML 404. Partner agreements UI would be **Blocked (env)** even with a valid partner session. Did not rebuild/fix the image.

## Counts

| Result | Count |
|--------|------:|
| Pass | 0 |
| Fail | 0 |
| Blocked | 2 |
| Skipped | 1 (`5.2` — student/coordinator denial; other agents) |

**Blocked:** 5.1, partner cold-open of `/seim/admin/programs` and `/seim/review-queue`  
**Skipped:** 5.2

---

## Section 5 — Partner portal

### 5.1 Partner portal (`partner`) — **Blocked** (seed + env)

Two independent blockers. Neither is a product Fail on this stack.

**A. Seed — partner account missing**

- Django admin Users (`/seim/django-admin/accounts/user/?all=`): `admin@test.com`, `coordinator@test.com`, `student@test.com`, `mmanzano@uadec.edu.mx`, `test@example.com`. Search `partner` → **0 users**.
- `POST /api/login/` `{"login":"partner@test.com","password":"partner123"}` → **401** `{"detail":"Invalid credentials"}`.
- Could not open `/seim/partner`, agreements table, **View documents**, or **Open thread**.

**B. Env — exchange URL stub (would block the portal APIs)**

- Admin JWT `GET /api/partner/agreements/` → `404` Wagtail HTML.
- `GET /api/exchange-agreements/` → same 404.
- Same root cause as Public **DEF-2026-08-16-01** (`/app/exchange/urls.py` stub). Host source registers `partner/agreements` on the exchange router.

**Repro (seed):**

1. Confirm users: Django admin → Users, search `partner@test.com` → 0 rows.
2. `curl.exe -H "Content-Type: application/json" --data-binary "{\"login\":\"partner@test.com\",\"password\":\"partner123\"}" http://localhost:8020/api/login/`
3. Response **401 Invalid credentials**. SPA Partner portal never loads.

**Repro (env, once a partner user exists):**

1. `curl.exe http://localhost:8020/api/partner/agreements/` (with partner Bearer) → HTML 404, not DRF JSON.
2. `docker compose -p seim-localprod -f docker-compose.local-prod.yml exec web cat /app/exchange/urls.py` → `urlpatterns = []`.

### 5.2 Non-partner denied — **Skipped**

Student/coordinator denial of `/seim/partner` is **§1.6** (other agents). Not re-run here. Admin navbar showed a **Partner portal** item while logged in as admin; that surface was not opened (would still hit the stub APIs).

---

## Partner must not open staff/admin URLs

Assignment: as partner, cold-open `/seim/admin/programs` and `/seim/review-queue`.

### Partner → `/seim/admin/programs` and `/seim/review-queue` — **Blocked** (could not authenticate as partner)

- No partner JWT/session (5.1 A).
- Client-side guards not observed for this role.
- **Not Fail.** Re-run after `seed_demo_readiness` (or equivalent partner user) **and** a web image with the real `exchange/urls.py`.

**Intended repro (for a later pass):**

1. Full logout. Confirm `/seim/login`.
2. Login `partner@test.com` / `partner123`.
3. Paste `http://localhost:8020/seim/admin/programs` — expect redirect away, no admin catalog.
4. Paste `http://localhost:8020/seim/review-queue` — expect redirect/deny, no review-queue chrome.

---

## Defects

No new product Fail unique to Partner. Recorded blockers:

### DEF-2026-08-16-01 (env) — Exchange API stub

Blocks `/api/partner/agreements/` (and thus 5.1 UI) on this image. Details in `2026-08-16-public.md` / admin run.

### DEF-2026-08-16-04 (seed) — `partner@test.com` missing

- **Cluster:** `partner` / `roles`
- **Symptom:** Partner demo account not in DB; JWT login 401.
- **Repro:** see 5.1 A.
- This DB looks like a thin Manual QA seed (admin/coordinator/student + two extra users), not full `seed_demo_readiness`.

---

## Notes

- Did not destroy demo data. Did not run Section 8.
- Did not edit `manual-qa-full-checklist.md`, `feature-tracking.md`, or `feature-test-tracking.md`.
- Student/coordinator must-not-see-partner-table checks are out of scope for this agent.
