# Manual QA rollup — 2026-08-16

**Date:** 2026-08-16  
**App:** `http://localhost:8020`  
**Stack:** Compose project `seim-localprod` (`docker-compose.local-prod.yml`), container `seim-web-local-prod`, settings `seim.settings.production`  
**Did not:** rebuild the image, fix product code, or re-run browsers.

**Role reports:**

- [Public](2026-08-16-public.md) (Section 0 + anonymous Section 1)
- [Student](2026-08-16-student.md) (Sections 1–3, 6.7/9 student, 9)
- [Coordinator](2026-08-16-coordinator.md) (Section 4)
- [Admin](2026-08-16-admin.md) (Sections 6, 7, 4.8)
- [Partner](2026-08-16-partner.md) (Section 5 + staff-URL denial)

**Section 8 (cross-role lifecycle) was not run.** It was blocked by stub exchange APIs (`/app/exchange/urls.py` is `urlpatterns = []`) and an unverified `student@test.com` (no student JWT session). No role agent executed that section.

**Retest (same day, after rebuild/seed/fixes):** [`2026-08-16-retest-summary.md`](2026-08-16-retest-summary.md). Env blockers cleared; MQ-001–003 closed. Thin §8 observed (seeded apps + coordinator can open detail); no new product Fails.

---

## Per-role counts

Counts are copied from each role report. Items overlap across reports (for example 0.8); do not sum the columns.

| Role | Pass | Fail | Blocked | Other |
|------|-----:|-----:|--------:|-------|
| Public | 5 | 1 | 4 | Not executed: 1 (`1.1`) |
| Student | 5 | 0 | 28 | Cross-role §4–§8 (except student half of 6.7 / 9.1), 1.3–1.4 not executed |
| Coordinator | 4 | 2 | 10 | Not executed: 1 (`4.8` — admin workload) |
| Admin | 9 | 1 | 2 | Skipped: 1 (`6.7` — student/coordinator) |
| Partner | 0 | 0 | 2 | Skipped: 1 (`5.2` — student/coordinator denial) |

**Pass lists (from reports):**

- Public: 0.1, 0.3, 0.7, 0.8, 1.3
- Student: 0.8, 0.9, 1.1, 2.16, 9.2
- Coordinator: 4.7, 4.11, 4.14, 4.15
- Admin: 6.2, 6.3, 6.4, 6.5, 4.8, 7.2, 7.3, 7.4, 7.5
- Partner: none

**Fail lists (from reports):**

- Public: 0.2 (Swagger/schema missing programs, applications, calendar paths; live those URLs are Wagtail HTML 404). Root cause is the image stub `exchange/urls.py` — **env**, not an MQ.
- Student: none
- Coordinator: signed-in coordinator reached `/seim/admin/programs`; signed-in coordinator reached `/seim/partner`
- Admin: 7.1 Django admin `ExchangeAgreement` changelist 500
- Partner: none

---

## Shared environment blockers

Not filed as `MQ-*`. Details in the role reports.

1. **Stub `/app/exchange/urls.py`** in the `seim-localprod` web image (`urlpatterns = []`). Host source has the SimpleRouter. Live `GET /api/programs/`, `/api/applications/`, `/api/calendar/events/`, `/api/exchange-agreements/`, `/api/partner/agreements/`, `/api/eligibility-rulesets/`, `/api/saved-searches/` return Wagtail HTML 404. OpenAPI omits those path groups. Blocks coordinator queue/agreements/nominations/rulesets, admin SPA programs/application edit, partner agreements API, and student §2–§3 even after login.
2. **Wagtail welcome / missing `/programas/`.** `/` is default “Welcome to your new Wagtail site!”; `/programas/` 404. `restore_cms` not run. Public 0.4–0.6 Blocked. Wagtail explorer itself passed (admin 7.2).
3. **Unverified `student@test.com`.** User exists, `is_active=True`, `is_email_verified=False`. Correct and wrong passwords both return `401` “Email not verified…”. No student session; §2–§3, signed-in 1.5/1.6/6.7/9.1, and Section 8 student half Blocked.
4. **Missing `partner@test.com`.** Users on this DB: `admin@test.com`, `coordinator@test.com`, `student@test.com`, `mmanzano@uadec.edu.mx`, `test@example.com`. JWT `POST /api/login/` with partner credentials → `401` Invalid credentials. Partner 5.1 and partner cold-open of staff URLs Blocked.

Coordinator login also failed with “Email not verified” until that account was set `is_email_verified=True` and `is_staff=True` for the coordinator pass only (no image rebuild, no `seed_demo_readiness`). Seed on this stack is a thin Manual QA set (5 users, 2 programs, 1 unrelated draft), not full `seed_demo_readiness`.

---

## Product fails (filed)

| ID | Summary | Source |
|----|---------|--------|
| **MQ-2026-08-16-001** | Coordinator can open `/seim/admin/programs` (expected deny) | [coordinator](2026-08-16-coordinator.md) |
| **MQ-2026-08-16-002** | Coordinator can open `/seim/partner` (expected deny) | [coordinator](2026-08-16-coordinator.md) |
| **MQ-2026-08-16-003** | Django admin `ExchangeAgreement` changelist 500 (`column exchange_exchangeagreement.required_gpa does not exist`) | [admin](2026-08-16-admin.md) 7.1 |

Log: [`manual-qa-issues.md`](../manual-qa-issues.md).

### MQ-2026-08-16-001 — Coordinator `/seim/admin/programs`

1. Log in at `http://localhost:8020/seim/login` as `coordinator@test.com` / `coordinator123` (account must be email-verified).
2. Open `http://localhost:8020/seim/admin/programs` (or sidebar **Admin — Programs** / App admin).
3. Stays on `/seim/admin/programs`, title “Admin — Programs”, catalog chrome (Program management, New program). URL does not change to `/seim/applications` or dashboard. List may show “Failed to load programs.” (stub API); the fail is the missing deny.

### MQ-2026-08-16-002 — Coordinator `/seim/partner`

1. Log in as `coordinator@test.com` / `coordinator123`.
2. Open `http://localhost:8020/seim/partner` (or sidebar **Partner portal**).
3. Stays on `/seim/partner`, title “Partner portal”. Sidebar **Partner portal** is shown to coordinator. “Failed to load partner portal.” may appear (stub APIs); the fail is the missing deny.

### MQ-2026-08-16-003 — Django admin agreements changelist 500

1. Sign in at `http://localhost:8020/seim/django-admin/login/` as `admin` / `admin123` (Django username, not the SPA email).
2. Open Exchange → **Exchange agreements** (`/seim/django-admin/exchange/exchangeagreement/`).
3. HTTP 500. Container log: `django.db.utils.ProgrammingError: column exchange_exchangeagreement.required_gpa does not exist`. Users and programs changelists returned 200.

---

## Notes

- Public 0.2 is recorded as Fail in the public report; it is the stub `exchange/urls.py` (env blocker 1), not a separate MQ.
- Student 0.9 Pass (Playwright) vs public 0.9 Blocked (no IDE browser) — same checklist item, different method.
- SPA login shell on 8020 is real Vue (`SEIM · 2.0.0-vue`), not “Vue build required”.
- `feature-tracking.md` was not edited. Cluster matrix in `feature-test-tracking.md` was not updated.
