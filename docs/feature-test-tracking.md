# SEIM feature ↔ test coverage matrix

_Companion to [`docs/feature-tracking.md`](feature-tracking.md). Manual QA defect IDs: [`manual-qa-issues.md`](manual-qa-issues.md). Manual workflow session log: [`manual-workflow-qa-session-log.md`](manual-workflow-qa-session-log.md). Use this file as the source of truth for **unit**, **smoke**, and **browser** coverage per feature or workflow cluster. Update rows when you add tests or re-verify._

## Status legend

| Symbol | Meaning |
|--------|---------|
| `—` | Not started / unknown |
| `Partial` | Some paths covered; list gaps in Notes |
| `Done` | Current suite green; paths recorded |

**Layers**

- **Unit**: Django `pytest` under `tests/unit/` and `tests/integration/api/` where appropriate; Vue `npm --prefix frontend-vue run test:run` (Vitest) for SPA logic and components.
- **Smoke**: Playwright tests marked `smoke` (`make e2e-smoke` with app reachable at `BASE_URL`).
- **Browser**: Playwright workflow or UI journeys (`tests/e2e_playwright/`), including `-m vue` / `make vue-e2e` when the SPA is under test.

## How to run (reference)

- Backend unit + integration (Docker): `make test-unit` and `make test-integration` (or `make test` / `./scripts/run_tests.sh` per project docs).
- Frontend unit: `npm --prefix frontend-vue run test:run`
- Playwright smoke: `make e2e-smoke` (expects `http://localhost:8001` unless overridden).
- Vue browser E2E: `make vue-e2e` (seeds + `BASE_URL`/`API_URL` per Makefile).

Adjust hosts and compose profiles to match your environment (`docker-compose.yml`, `CLAUDE.md`).

---

## Matrix (feature / workflow clusters)

_Map each row to one or more rows in `docs/feature-tracking.md` § **IMPLEMENTED** (or **IN PROGRESS**). Prefer updating an existing row over adding duplicates._

| Cluster ID | Feature / workflow (tracker reference) | Unit | Smoke | Browser | Last verified | Notes |
|------------|----------------------------------------|------|-------|---------|---------------|-------|
| `auth-api` | User authentication and account management | Done | Partial | Partial | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`):** **Blocked** for in-browser JWT/login UI — `/seim/login` serves “Vue build required” fallback (no SPA assets). **Partial:** OpenAPI still reachable at `/api/docs/` in prior tab. Defect/ref: **MQ-2026-04-09-001** (`manual-qa-issues.md`). **Manual QA (2026-04-09, second pass, browser MCP, `http://localhost:8001`):** `/seim/login` loads full Vue shell (Sign In, skip link; not build-required on this host). `student1@example.com` / `student123` → `POST /api/accounts/login/` **401** when user not seeded (**Blocked** for post-login UI; use E2E seed or admin-created user). **Fail (a11y/UX):** after 401, no error in accessibility tree (console only) — **MQ-2026-04-09-006**. **Fail (account switch):** after successful `student@test.com` login, `coordinator@test.com` via `/seim/login` → **`POST /api/accounts/login/`** **403** — **MQ-2026-04-09-007**. **Unit (green):** `tests/integration/api/test_auth_api.py` (26 tests, Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`, `pytest -o addopts='--strict-markers --tb=short'`); `tests/unit/api/test_api_views.py` (`test_token_obtain_pair`, `test_token_refresh`); Vue `frontend-vue/src/stores/auth.spec.js` (`npm run test:run -- --run src/stores/auth.spec.js`). **Smoke/Browser:** Playwright `-m smoke` not re-run here: `Dockerfile.test` image lacks Chromium system libs after `playwright install`; use **host** `make e2e-smoke` with app at `BASE_URL` (see Makefile). Smoke markers include `test_smoke.py`, `test_auth_workflows.py::TestAuthenticationSmoke::test_login_logout_smoke` (needs Vue users + `BASE_URL` e.g. 5173). **Browser:** `test_auth_workflows.py` (registration/login/logout; `-m auth`). |
| `roles` | Role management (student / coordinator / admin) | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `tests/unit/core/test_core_permissions.py`, `tests/unit/accounts/test_accounts_management_commands_assign_user_roles.py`, `tests/unit/accounts/test_accounts_models.py` (56 tests, Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`). No dedicated Vue route-guard spec file; Pinia `auth` + staff flags exercised in view specs. **Manual QA (2026-04-09, browser MCP):** Anonymous **`/seim/review-queue`** → login with `redirect=/review-queue` (**Pass**). Switching role via login form while already signed in **blocked** by **403** on login API — **MQ-2026-04-09-007**. **Smoke/Browser:** host Playwright role-shaped journeys: `test_student_workflows.py`, `test_coordinator_workflows.py`, `test_admin_workflows.py` (`-m student` / `coordinator` / `admin`). |
| `programs-applications` | Exchange programs and application workflow | Done | Partial | Partial | 2026-04-08 | **Manual QA (2026-04-09, env: browser MCP + `curl.exe`, Docker `web` on `http://localhost:8001`):** **Partial.** **Pass:** `/api/docs/` exposes **programs** and **applications** tags (list/detail and related ops visible in Swagger). Anonymous **`GET /api/programs/`** and **`GET /api/applications/`** → **401** with `WWW-Authenticate: Bearer` (`curl.exe -sI`) — consistent with authenticated read on programs (`IsAdminOrReadOnly`) and applications (`IsAuthenticated` / `IsStudentOrReadOnly`). Browsable **`/api/programs/`** in browser shows DRF *Program List* shell (401 HTML). **`/seim/applications`** without session → redirect **`/seim/login?redirect=/applications`**; login form (email/password, Sign In) loads — **not** the *Vue SPA assets missing* page in this compose run. **Not exercised:** authenticated student submit/draft/timeline (needs JWT or UI login + seed users). **Unit (green):** `tests/integration/api/test_applications_api.py` (23 tests, incl. `api:comment-list` POST + comment XSS/filter cases), `test_programs_api.py`; `tests/unit/exchange/test_application_submission.py`, `test_application_readiness.py` (`docker compose exec web … pytest -o addopts='--strict-markers --tb=short'` + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **Timeline events (application detail API):** `tests/unit/exchange/test_timeline_events_api.py` — `GET /api/timeline-events/?application=` + `ordering` returns `created_by_name`; student cannot see another student's timeline (empty list); re-verify: `docker compose exec web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/unit/exchange/test_timeline_events_api.py -o addopts='--strict-markers --tb=short'"` (2 passed). **`test_programs_api.py` (integration):** `api:program-list` / detail — auth, admin-only CUD, validation, filter/order/pagination, **search**, workflow, eligibility, stats, performance, access-control. **Full re-verify (Postgres):** `docker compose exec web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/integration/api/test_programs_api.py -o addopts='--strict-markers --tb=short'"` (17 passed when `web` keeps compose `DATABASE_URL` → test DB on `db` service). **SQLite-only host:** `docker compose exec -e DATABASE_URL= web sh -c "… pytest tests/integration/api/test_programs_api.py -k 'not search' …"` (15 passed); `test_program_search` + `test_program_search_performance` fail on SQLite (`OperationalError: unrecognized token: "@"`). **Enrollment / waitlist:** `tests/unit/exchange/test_exchange_services.py::TestApplicationService` — `test_can_submit_application_existing_waitlist`, `test_submit_application_waitlist_when_at_capacity`, `test_submit_application_raises_when_at_capacity_no_waitlist`; re-verify: `pytest …/test_exchange_services.py -k 'existing_waitlist or waitlist_when_at_capacity or raises_when_at_capacity_no_waitlist'` (3 passed). **Coordinator assignment:** `tests/unit/exchange/test_exchange_serializers.py` — `test_program_serializer_rejects_non_coordinator_assignment`, `test_application_serializer_exposes_assigned_coordinator`, `test_student_cannot_override_assigned_coordinator`, `test_coordinator_can_assign_application_coordinator_on_update`; `tests/unit/exchange/test_application_dynamic_form_api.py` — `test_create_application_with_dynamic_form_submission` (auto-assign when single program coordinator), `test_student_cannot_set_assigned_coordinator_directly` (6 passed, same Docker/pytest settings). Vue: `Applications.spec.js` (list + sparse + `applicationDetailPage` labels + `pagination.*` multi-page), `ApplicationDetail.spec.js` (comments thread + submit + i18n + `applicationDetailPage.notAvailable` sparse program), `applicationReadiness.spec.js`. **Smoke/Browser:** same host Playwright constraint as `auth-api`; journeys `test_student_workflows.py` (`-m student` `-m workflow`), coordinator/admin flows touch applications. |
| `documents-core` | Document management + checklist + preview | Done | Partial | Partial | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP + `curl.exe`, Docker `http://localhost:8001`):** **Partial.** **Pass:** `/api/docs/` lists **documents** (CRUD + **`GET …/{id}/preview/`** + `validate_document`) and **agreement-documents** (list/create/detail). Anonymous **`HEAD /api/documents/`**, **`/api/agreement-documents/`**, **`/api/documents/00000000-0000-0000-0000-000000000000/preview/`** → **401** + `WWW-Authenticate: Bearer`. **`/seim/documents`** without session → **`/seim/login?redirect=/documents`** with sign-in form (**Pass**). **Not exercised:** upload, checklist on application detail, preview with real file + JWT. **Document preview (integration API):** `tests/integration/api/test_document_preview_api.py` — `GET api:document-preview` inline PDF (`Content-Type` / `Content-Disposition`), student owner + coordinator OK, other student 404; re-verify: `docker compose exec web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/integration/api/test_document_preview_api.py -o addopts='--strict-markers --tb=short'"` (3 passed). **Unit (green):** `test_documents_list_filters.py`; `tests/unit/exchange/test_document_checklist_api.py`; `tests/unit/documents/` (125 passed, 1 skipped — `docker compose exec web … pytest -o addopts='--strict-markers --tb=short'` + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **Resubmission:** `tests/unit/documents/test_documents_services_main.py` — `DocumentService.can_request_resubmission` + `request_resubmission` (limits / success / over-limit); re-verify: `pytest …/test_documents_services_main.py -k resubmission` (4 passed). Review-queue filter `needs_document_resubmit` → `coord-review` (`test_filters.py`). Vue: `Documents.spec.js` (list + sparse + `documentDetailPage` status + `pagination.*` multi-page), `DocumentDetail.spec.js` (incl. fallbacks), `DocumentUpload.spec.js`. **Smoke:** `test_document_workflows.py` (`TestDocumentWorkflowsSmoke::test_documents_page_smoke`, `-m smoke`); host Playwright / image browser libs same as `auth-api`. **Browser:** `test_document_workflows.py` (`-m document` where used). Agreement-doc REST: `tests/integration/api/test_agreement_documents_api.py` (run with `agreements` slice). |
| `dynamic-forms` | Dynamic form builder + Vue consumption + steps / branching | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `tests/unit/application_forms/` (incl. **`test_step_templates.py`** — `FormStepTemplate` + `apply_step_template_to_form_type`, admin `formtype-apply-step-template` API incl. slug; 7 tests, re-verify: `pytest …/test_step_templates.py`; **`test_conditional_visibility.py`** — field + step `visible_when` / `x-seim-visibleWhen`, program context, `staff_only` / `roles_any`, merged step-patch + required-when-hidden; 20 tests, re-verify: `pytest …/test_conditional_visibility.py`); `tests/unit/exchange/test_application_dynamic_form_api.py` (incl. **multi-step + per-step docs gate:** `test_multistep_partial_create_succeeds`, `test_multistep_requires_current_step_key`, `test_submit_fails_until_multistep_form_complete`, `test_multistep_advancing_blocked_until_step_documents_approved`; re-verify: `pytest …/test_application_dynamic_form_api.py -k multistep` — 4 passed); `test_exchange_services_additional.py::TestDynamicFormProcessing`, `::TestProcessDynamicFormStepVisibleWhen`; `tests/integration/test_dynamic_forms_comprehensive.py` (152 tests, Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`). Vue: `ApplicationForm.spec.js` (incl. `applicationFormPage.programSearchPlaceholder`, `applicationFormPage.notAvailable` missing dates); **`dynamicFormVisibility.spec.js`** — `fieldMeetsVisibleWhen` / `stepMeetsVisibleWhen` / `visibleWhenRuleMatches` (program context, `staff_only`; 7 Vitest, `npm run test:run -- --run src/utils/dynamicFormVisibility.spec.js`). `FormType.created_by` now `blank=True` + migration `0005_alter_formtype_created_by_blank` (aligns `full_clean()` with optional creator). **Smoke/Browser:** host Playwright; `test_vue_ui.py` (`-m vue`), student/coordinator flows may cover wizard. |
| `notifications` | Notifications center, real-time, digests, routing reference | Done | Partial | Partial | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`, Vite `:5173` unreachable):** **Blocked** for Staff Vue **`/notification-routing`** and Settings staff-only link — `/seim/login` shows *Vue SPA assets missing*; dev SPA not running on 5173. **Partial:** `/api/docs/` **Pass** — Swagger lists **GET `/api/notifications/routing-reference/`** (notifications group). Same env class as resolved build path **MQ-2026-04-09-001**; follow `README` / Dockerfile `dist` or run Vite for full UI pass. **Manual QA (2026-04-09, third pass, browser MCP, Vue on `http://localhost:8001`):** Anon **`GET /api/notifications/routing-reference/`** → **401** (`curl.exe`). Anonymous **`/seim/notification-routing`** → **`/seim/login?redirect=/notification-routing`** (**Pass**). Staff **`NotificationRouting`** page **Blocked** in-session when attempting coordinator login after student: **`POST /api/accounts/login/`** **403** — **MQ-2026-04-09-007** ([`manual-qa-issues.md`](manual-qa-issues.md)). **Unit (green):** 148 tests — `tests/unit/notifications/` (incl. `test_notification_center.py`, `test_websocket_consumer.py` (connect, mark-read, ping, broadcast, disconnect; **`application.sync` → client** in **NotificationConsumer.application_sync** slice), `test_notification_digest.py`, digest/tasks/services). **`Application sync broadcast (unit):** `tests/unit/notifications/test_application_sync_broadcast.py` — `NotificationService.broadcast_application_sync` sends `application.sync` with `application_id` + `change_type` to notification groups (student + program coordinator; mocked channel layer, two `group_send` calls); re-verify: `docker compose exec web sh -c "unset DATABASE_URL && export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/unit/notifications/test_application_sync_broadcast.py -o addopts='--strict-markers --tb=short'"` (1 passed). **`NotificationConsumer.application_sync` (unit):** `tests/unit/notifications/test_websocket_consumer.py` — `test_application_sync_forwards_to_client` forwards `application.sync` channel events to the client WebSocket as JSON (`application_id`, `change_type`, `document_id`); re-verify: `docker compose exec web sh -c "unset DATABASE_URL && export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/unit/notifications/test_websocket_consumer.py -k application_sync_forwards -o addopts='--strict-markers --tb=short'"` (1 passed). **`Digest (`process_notification_digests`):** same code path as Celery `notifications.tasks.send_notification_digests` and `manage.py send_notification_digests`; `test_notification_digest.py` — `skipped_no_unread`, digest notification + `notification_digest_last_sent_at`, `skipped_cadence` (daily), `notification_type` `both` + `settings_category=system` when email digest enabled, `skipped_suppressed` (no channel) with timestamp still advanced; re-verify: `pytest tests/unit/notifications/test_notification_digest.py` (5 passed, Docker + `unset DATABASE_URL` + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **`test_notifications_tasks.py` (deadline reminders):** `settings_category_for_reminder_event` (event type → UserSettings group for `send_deadline_reminders`) + `send_deadline_reminders` (`TestSendDeadlineRemindersTask`); re-verify: `pytest …/test_notifications_tasks.py -k 'settings_category_for_reminder or send_deadline_reminders_no_reminders or send_deadline_reminders_one_reminder'` (11 passed). **`test_notifications_services.py` — `settings_category`:** **comments** (`email_comments` vs document email), **programs** (`inapp_programs` / `email_programs`), **system** (`inapp_system`); re-verify: `pytest …/test_notifications_services.py -k 'usersettings_comments or usersettings_programs or usersettings_system_inapp'` (6 passed); **`Routing reference (unit):** `tests/unit/notifications/test_routing_reference.py` — `build_notification_routing_reference()` (`schema_version` 12, settings categories + digest block, transactional/reminder indices, route `settings_category` validity, reminder descriptions/recipient summaries); re-verify: `pytest tests/unit/notifications/test_routing_reference.py` (6 passed, Docker + `unset DATABASE_URL` + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **`Routing reference (integration API):** `tests/integration/api/test_notification_routing_reference_api.py` — `GET api:notification-routing-reference` unauth 401/403, student 403, coordinator 200 + `schema_version` 12 + settings categories / reminder + transactional indices / digest; re-verify: `docker compose exec web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/integration/api/test_notification_routing_reference_api.py -o addopts='--strict-markers --tb=short'"` (3 passed). **WebSocket notifications (integration):** `tests/integration/test_websocket_notifications.py` — `WebsocketCommunicator` + `NotificationConsumer`: authenticated connect + `connection_established`, anonymous rejected, notification broadcast, mark read / mark all read, ping/pong, error handling; sync coverage for `NotificationService` broadcast helpers; re-verify: `docker compose exec web sh -c "unset DATABASE_URL && export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/integration/test_websocket_notifications.py -o addopts='--strict-markers --tb=short'"` (9 passed). **Re-verified slice:** `pytest …/test_application_sync_broadcast.py …/test_notification_digest.py` (6 passed, Docker + `seim.settings.test`). Run full suite in Docker: `sh -c "unset DATABASE_URL && export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest … -o addopts='--strict-markers --tb=short'"` so SQLite in-memory is used; `pytest-asyncio` in `Dockerfile` web image; `pytest.ini` `asyncio_mode = auto` + `asyncio` marker. Vue: `Notifications.spec.js` (5 tests, incl. `notifications.breadcrumbAria`, `clearFilters`, filter/loading keys, `notAvailable`, multi-page `pagination.*`), `NotificationRouting.spec.js` (6 Vitest); `websocket.spec.js` (`application.sync` → `seim-application-sync`, 4 Vitest — `npm run test:run -- --run src/services/websocket.spec.js`). **Smoke/Browser:** host Playwright not re-run; WebSocket/inbox journeys same constraint as `auth-api`. |
| `grades` | Grade translation | Done | — | — | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`):** **Pass** — OpenAPI (`/api/docs/`) exposes **grades** group with `GET/POST /grades/api/scales/`, `…/values/`, `…/translations/` (incl. `/scales/active/`). **Checked paths:** navigated `GET /grades/api/scales/` (DRF browsable title *Grade Scale List*) and `?format=json` (JSON view loads in browser). **N/A:** no dedicated SPA grades UI. **Adjacent:** `/seim/login` still *Vue SPA assets missing* (same class as **MQ-2026-04-09-001** / env); unrelated to grades API. **Unit (green):** `tests/unit/grades/test_grades_services.py`, `tests/integration/api/test_grades_api.py` (56 tests, Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`). No dedicated Vue grades specs. **Smoke/Browser:** `—` by design (no grades-only pages); may appear indirectly via profile/program E2E. |
| `analytics` | Analytics dashboard + exports (CSV/XLSX/PDF) | Done | Partial | Partial | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`):** **Partial.** **Pass:** `/api/docs/` — **GET `/api/analytics/export/`** documented; operation expanded; **`export_format`** visible on page (Swagger). **`GET /health/`** reachable. **Fail:** **`GET /admin-dashboard/`** → **404** (legacy admin analytics / export UI not served). **Defect:** **MQ-2026-04-09-005** ([`manual-qa-issues.md`](manual-qa-issues.md)). **Note:** `/seim/admin/login/` with E2E doc credentials did not complete login in this env (treated as env/seed, not a filed bug). **Unit (green):** `tests/unit/analytics/` (151 passed, 1 skipped — Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **REST exports:** `test_analytics_views_simple.py` `TestAdvancedAnalyticsAPIViews` — `api:analytics-export` default CSV + `export_format=xlsx` + `export_format=pdf` (`test_export_api_returns_*`); slice re-run: `pytest …/test_analytics_views_simple.py -k export_api_returns` (3 passed). **Harness notes:** `seim/settings/test.py` drops Wagtail `context_processors` when Wagtail stripped; `templates/base.html` `{% url "api:token_refresh" %}`; `tests/conftest.py` `another_user`; analytics cache/program-stats mocks as in suite. **Smoke/Browser:** host Playwright `test_admin_workflows.py` (`-m admin`); admin dashboard templates. |
| `vue-portal` | Vue student/coordinator portal (shell, routing, a11y) | Done | Partial | Partial | 2026-04-09 | **Unit (green, Vitest):** `App.spec.js`, `Dashboard.spec.js`, `Login.spec.js`, `Profile.spec.js` (4 tests, incl. `profilePage.breadcrumbAria`, loading spinner, `gpaPlaceholder`, `cancel` en/es — `npm run test:run -- --run src/views/Profile.spec.js`), `NotFound.spec.js`, `utils/a11y.spec.js`, `utils/documentTitle.spec.js` (i18n browser tab titles), `components/ToastContainer.spec.js`, `components/NotificationDropdown.spec.js`, `i18n/index.spec.js`, `utils/dashboardNextSteps.spec.js` (merge + `fetch` + locale-aware titles), **`services/api.spec.js`** — shared axios client default timeout / JSON headers / `withCredentials`, request+response interceptors, `baseURL` string (3 tests, `npm run test:run -- --run src/services/api.spec.js`), `services/uiPreferences.spec.js`, `services/websocket.spec.js` (`npm run test:run -- --run` with those paths). **Dashboard stats (integration API):** `tests/integration/api/test_dashboard_stats_api.py` — `GET api:accounts:dashboard_stats` unauth 401/403, student sees own application/pending counts, coordinator sees all applications + staff pending; re-verify: `docker compose exec web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/integration/api/test_dashboard_stats_api.py -o addopts='--strict-markers --tb=short'"` (3 passed, compose `web` + Postgres test DB). **Profile `additional_languages` (API/serializer):** `tests/unit/accounts/test_profile_additional_languages.py` (4 tests, Docker + `seim.settings.test`). Feature views also have specs under other clusters. **Smoke/Browser:** host Playwright `test_vue_ui.py` (`-m vue`); same Docker browser-lib constraint as `auth-api`. |
| `cms-public` | Wagtail CMS public site | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `tests/unit/cms/` — 9 tests with **`DJANGO_SETTINGS_MODULE=seim.settings.test_cms`** (Wagtail + `cms` enabled; `seim/settings/test_cms.py` extends `test` but keeps full `INSTALLED_APPS` minus `cacheops`): `test_program_discovery.py` (program index search/filters + active-only + filter options); `test_exchange_program_sync.py` (draft `ProgramPage` link, sync/publish, duplicate guard, missing page, Wagtail edit URL). Re-verify: `docker compose exec web sh -c "unset DATABASE_URL && export DJANGO_SETTINGS_MODULE=seim.settings.test_cms && pytest tests/unit/cms/ -o addopts='--strict-markers --tb=short'"` (9 passed). Default `seim.settings.test` **cannot** collect these (Wagtail stripped). **Smoke:** `tests/e2e_playwright/test_smoke.py::test_homepage_loads` (and mobile variant) when `BASE_URL` is the site root — **host** Playwright / same Docker browser constraint as `auth-api`. **Browser:** broader marketing/CMS journeys often manual or video demos; not re-run here. **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`):** **Pass** (public CMS slice). Loaded `/` (hero, nav, FAQ accordion), `/programas/` (search, location filter, “3 programs found”, compare CTA), `/programas/bologna-italia/` (detail headings, apply CTA region). **Not tested:** signed-in compare / SPA apply (blocked by MQ-2026-04-09-001). **Cosmetic:** footer `© 2025` on public pages — see `manual-qa-issues.md`. |
| `url-routing` | Global URL map (root vs `/seim/` SPA, admin redirects, API) | Done | Partial | — | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`):** **Partial.** `/api/docs/` **Pass** (SEIM API Swagger loads). `/` **Pass** (CMS home). `/health/` reachable. `/seim/login` still **Vue SPA assets missing** on this host (same class as resolved MQ-001 — rebuild/mount `dist` or use `:5173`). `/contact/` **Fail** — plain-text “No contact form configured…” (see **MWL-2026-04-09-001** / **MWL-2026-04-09-002** in [`manual-workflow-qa-session-log.md`](manual-workflow-qa-session-log.md)). **Manual QA (2026-04-09, second pass, same env):** `/seim/login` **Pass** (Vue SPA login form). `/contact/` **Pass** — full site shell (`Contact` title, nav, footer `© 2026`); body message “No contact form configured…” (expected without Wagtail form). **`GET /admin-dashboard/`** **404** — **MQ-2026-04-09-005** (see `analytics` cluster). **URL map:** `tests/test_urls.py` — `/` public (no `#app`), `/seim/` Vue shell, `/django/admin/` → `/seim/admin/`, Wagtail admin path, API + auth route registration, `/contact/` not caught by SPA, SPA catch-all vs `/seim/admin` / CMS / `/api` exclusions; re-verify: `docker compose exec -e DATABASE_URL= web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/test_urls.py -o addopts='--strict-markers --tb=short'"` (13 passed). **Core views:** `tests/unit/core/test_core_views.py` — `TestHealthCheckView` (`/health/` JSON + DB/cache status + mocked unhealthy paths); `TestContactFormView` / `TestContactFormSubmitView` (`/contact/`, `/contact/submit/` with active Wagtail form vs “No contact form configured”, inactive-only, validation); re-verify: `pytest tests/unit/core/test_core_views.py::TestHealthCheckView tests/unit/core/test_core_views.py::TestContactFormView tests/unit/core/test_core_views.py::TestContactFormSubmitView` (19 passed; `docker compose exec -e DATABASE_URL= web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest … -o addopts='--strict-markers --tb=short'"`). `seim/urls.py` mounts `core.urls` before Wagtail/frontend ``""`` includes so `/contact/` resolves. **OpenAPI (tracker: schema docs):** `tests/unit/api/test_api_views.py::TestAPIViews` — `test_api_schema` (`GET /api/schema/`), `test_api_docs` (`GET /api/docs/`); re-verify: `pytest …::test_api_schema …::test_api_docs` (2 passed, Docker + `seim.settings.test`). JWT/program smoke in same file → see `auth-api`. **Smoke:** `tests/e2e_playwright/test_smoke.py::test_api_health_check` (`/health/`) when host Playwright + `BASE_URL` — not re-run in Docker image here. **Browser:** `—` (no dedicated Playwright for Swagger UI). |
| `coord-review` | Coordinator review queue + workload + saved searches | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `tests/unit/exchange/test_filters.py` (program + application + review-queue filters), `tests/integration/api/test_coordinator_workload_api.py`, `tests/unit/exchange/test_saved_searches.py` (34 tests, Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`, `pytest -o addopts='--strict-markers --tb=short'`). Shared fixtures: `tests/conftest.py` adds `user_student` / `user_coordinator` aliases → `student_user` / `coordinator_user`. Vue: `reviewQueuePresets.spec.js`, `CoordinatorReviewQueue.spec.js` (incl. `reviewQueuePage.presetNamePlaceholder`, `reviewQueuePage.status` / sort / `clearFilters` en/es, multi-page `pagination.*`), `CoordinatorWorkload.spec.js` (2 Vitest: en subtitle + `workloadPage.pageSubtitleStatus*` es), `useStaffSavedPresets.spec.js`, `staffListSearchPresets.spec.js` (11 tests). **Smoke/Browser:** host Playwright; `test_coordinator_workflows.py` (`-m coordinator` `-m workflow`). |
| `agreements` | Exchange agreements + documents + renewal + expiration reminders | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `tests/integration/api/test_exchange_agreements_api.py`, `test_agreement_documents_api.py`; `tests/unit/exchange/test_exchange_agreement.py`, `test_agreement_renewal.py`, `test_agreement_expiration_reminders.py`. **Agreement document (unit):** `tests/unit/documents/test_exchange_agreement_document.py` — `ExchangeAgreementDocumentFilter` `current_only` (superseded excluded), `ExchangeAgreementDocumentSerializer` rejects cross-agreement `supersedes`; re-verify: `pytest tests/unit/documents/test_exchange_agreement_document.py` (2 passed, Docker + `unset DATABASE_URL` + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **ExchangeAgreement (unit):** `tests/unit/exchange/test_exchange_agreement.py` — `full_clean` rejects `end_date` before `start_date`, `__str__`, `ExchangeAgreementFilter` `expiring_within_days` (active only, within horizon); re-verify: `pytest tests/unit/exchange/test_exchange_agreement.py` (3 passed; when compose `web` sets `DATABASE_URL`, force SQLite via `docker compose exec -e DATABASE_URL= web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest tests/unit/exchange/test_exchange_agreement.py -o addopts='--strict-markers --tb=short'"`). **Agreement documents (integration API):** `tests/integration/api/test_agreement_documents_api.py` — `api:agreement-document-list` student 403, coordinator multipart create, `current_only` + `agreement` query (3 passed, same `-e DATABASE_URL=` + `seim.settings.test` harness). **Renewal (REST):** `test_exchange_agreements_api.py::ExchangeAgreementsAPITests` — `test_mark_renewal_pending_action` (`api:exchange-agreement-mark-renewal-pending`), `test_create_renewal_successor_action` (`api:exchange-agreement-create-renewal-successor`); re-verify: `pytest …/test_exchange_agreements_api.py -k 'mark_renewal_pending or create_renewal_successor'` (2 passed). **Renewal (service):** `tests/unit/exchange/test_agreement_renewal.py` — `AgreementRenewalService.mark_renewal_pending` (→ `RENEWAL_PENDING` + `renewal_follow_up_due`, rejects draft source), `create_renewal_successor` (draft + `renewed_from`, program M2M carry-over, document rollover when `copy_documents`, blocks second draft successor); re-verify: `pytest tests/unit/exchange/test_agreement_renewal.py` (4 passed, Docker + `unset DATABASE_URL` + `DJANGO_SETTINGS_MODULE=seim.settings.test`). **Expiration reminders:** `tests/unit/exchange/test_agreement_expiration_reminders.py` — `process_agreement_expiration_reminders` (90d milestone, idempotent `AgreementExpirationReminderLog`), linked-program coordinators, `recipients_for_agreement` excludes students; re-verify: `pytest …/test_agreement_expiration_reminders.py` (3 passed). Vue: `StaffExchangeAgreements.spec.js` (incl. `exchangeAgreementsPage.presetNamePlaceholder`, multi-page `pagination.*`), `StaffAgreementDocuments.spec.js` (incl. `staffAgreementDocumentsPage.presetNamePlaceholder`, multi-page `pagination.*`). **Smoke/Browser:** host Playwright; staff agreement/document lists overlap `staffListSearchPresets` / coordinator flows. |
| `calendar-ics` | Calendar events + deadlines + ICS subscribe | Done | Partial | Partial | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP + `curl.exe`, `http://localhost:8001`):** **Partial.** **Pass:** `/api/docs/` lists **calendar** tag with `GET /api/calendar/events/`, `GET /api/calendar/events/{id}/`, `GET /api/calendar/events/subscribe-token/`; browsable **`GET /api/calendar/events/`** loads (*Calendar Event List* — DRF). **`GET /api/calendar/events/subscribe-token/`** without JWT → **401** (`curl.exe`). **`GET /api/calendar/subscribe.ics`** without token → **403** (`curl.exe`). **Blocked (UI) when `dist` missing:** **`/seim/calendar`** may show *Vue SPA assets missing* (**MQ-001** class). **Manual QA (2026-04-09, third pass, browser MCP, `http://localhost:8001`, `student@test.com` via `docker compose exec web python manage.py create_vue_test_users`):** **`/seim/login?redirect=/calendar`** → JWT login → **`/seim/calendar`** **Pass** — Deadlines UI (HTTPS + webcal subscribe fields, range, type toggles, saved-view controls, event list). **Unit (green):** `tests/unit/exchange/test_calendar_api.py` (21 tests: events list filters, subscribe token, `.ics` — Docker + `DJANGO_SETTINGS_MODULE=seim.settings.test`). Vue: `DeadlinesCalendar.spec.js` (incl. `calendarPage.presetNamePlaceholder`). Calendar saved-filter (de)serialization: `staffListSearchPresets.spec.js` (see `coord-review`). **Smoke/Browser:** host Playwright; Dashboard **Deadlines** / SPA `/calendar` via `test_vue_ui.py` or manual. |
| `data-management` | Staff data management + bulk student import | Done | Partial | Partial | 2026-04-09 | **Manual QA (2026-04-09, env: browser MCP, `http://localhost:8001`):** **Partial.** Earlier run: index **404** + demo-data path login redirect (**MQ-2026-04-09-004**, now **resolved** in [`manual-qa-issues.md`](manual-qa-issues.md)). **Re-check (2026-04-09, Docker `web` up):** anonymous **`HEAD /data-management/`** → **302** `Location: /login/?next=/data-management/` — hub requires login (**Pass**). **Unit (green):** `data_management/tests.py` — `DataManagementViewTests` (bulk/export/import/demo/reset/cleanup) + `DataManagementAdminTests` queryset scoping; re-verify: `docker compose exec -e DATABASE_URL= web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest data_management/tests.py -o addopts='--strict-markers --tb=short'"` (16 passed, SQLite — `-e DATABASE_URL=` when `web` has Postgres `DATABASE_URL`). **Bulk student CSV import:** `data_management/tests.py` — `execute_import` with CSV creates student + profile (`language` / `language_level` / `gpa`) + `DataOperationLog`; second test updates existing row by email; re-verify: `docker compose exec -e DATABASE_URL= web sh -c "export DJANGO_SETTINGS_MODULE=seim.settings.test && pytest data_management/tests.py -k 'execute_import_queues_log_with_file or execute_import_updates_existing_student_by_email' -o addopts='--strict-markers --tb=short'"` (2 passed). **Discovery:** `pytest.ini` `testpaths` includes `data_management` (with `tests`), so `docker compose exec web pytest` collects these; `scripts/run_tests.sh` **backend unit** runs `pytest tests/unit/ data_management/tests.py`. Covers CSV student import create/update, `DataOperationLog`, `DataPermission`. **Smoke/Browser:** host Playwright not mapped; staff Django UI + admin. |
| `settings-ui` | User settings (appearance, notifications, i18n) | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `Settings.spec.js` (5 tests): load/save, UI language `LOCALE_STORAGE_KEY`, staff notification-routing link; form + sidebar strings via `settings.*` (incl. `settings.cancel`) / `route.names`. **Smoke:** host Playwright + `BASE_URL`. **Browser:** `test_student_workflows.py::test_view_settings`; POM `settings_page.py`. **Manual QA (2026-04-09, env: browser MCP, backend http://localhost:8001):** **Blocked** when `/seim/login` shows *“Vue SPA assets missing”* (**MQ-001** / missing `dist`). **Manual QA (2026-04-09, third pass, same URL, `student@test.com`):** **`/seim/settings`** after login **Pass** — Appearance (interface language, theme, font size, high contrast, reduced motion), Notifications (email/in-app matrix, digest, email digest disabled until system email on), Privacy; breadcrumbs + Save / Cancel / Go to Profile. Staff-only link to notification routing **absent** for student (**Pass**). |
| `readiness-compare` | Application readiness, program filters, compare | Done | Partial | Partial | 2026-04-09 | **Unit (green):** `tests/unit/exchange/test_application_readiness.py` + `tests/unit/exchange/test_filters.py::TestProgramFilter` (11 tests, Docker — 3 readiness + 8 program filters). **`accepting_applications=true`:** `test_filter_accepting_applications` (apply window open/deadline logic; backs `GET /api/programs/?accepting_applications=true`); re-verify: `pytest …/test_filters.py::TestProgramFilter::test_filter_accepting_applications`. Vue `applicationReadiness.spec.js`, `applicationProgramFilterPresets.spec.js`, **`programCompareQuery.spec.js`** — `parseCompareIdsFromQuery` / `compareIdsToQueryParam` / `MAX_COMPARE_PROGRAMS` (2 Vitest, `npm run test:run -- --run src/utils/programCompareQuery.spec.js`), `ProgramCompare.spec.js` (2 Vitest; `programComparePage.newApplication` / `emDash`). **Manual QA (2026-04-09, browser MCP, `http://localhost:8001`, `student@test.com`):** **`/seim/programs/compare`** **Pass** — compare heading, program checkboxes (6 demo programs), Clear selection, New application link, breadcrumb. Overlap: full `test_filters.py` / `ApplicationDetail` specs also listed under `coord-review` / `programs-applications`. **Smoke/Browser:** host Playwright `test_vue_ui.py` / new-application & compare flows. |

_Add rows for any **IN PROGRESS** or high-risk **IMPLEMENTED** feature from the tracker that is not represented above._

---

## Workflow → suggested Playwright entrypoints

_Use these as hints when filling the **Browser** column; replace with exact `test_file.py::test_name` once stable._

| Journey | Typical markers / files |
|---------|-------------------------|
| Critical path sanity | `tests/e2e_playwright/test_smoke.py`, `-m smoke` |
| Auth | `test_auth_workflows.py`, `test_auth_simple.py`, `-m auth` |
| Student | `test_student_workflows.py`, `-m student` |
| Coordinator | `test_coordinator_workflows.py`, `-m coordinator` |
| Admin | `test_admin_workflows.py`, `-m admin` |
| Vue SPA | `test_vue_ui.py`, `-m vue` |
| Accessibility | `-m accessibility` |

---

*Last updated: 2026-04-09 — **`notifications`:** `test_websocket_consumer.py` `application_sync_forwards` (1 pytest). Prior: **`data-management`** CSV import `-k` slice; staff list Vitest `pagination.*`.*
